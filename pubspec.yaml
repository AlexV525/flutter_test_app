name: testapp
description: A new Flutter application.

version: 1.0.0+2

environment:
  sdk: '>=2.10.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  ff_annotation_route: ^3.2.0

flutter:
  uses-material-design: true
  assets:
    - assets/
