name: testapp
description: A new Flutter application.

version: 1.0.0+2

environment:
  sdk: '>=2.10.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  photo_manager: 0.5.8 # Using this to generate a single `Podfile`.

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/
