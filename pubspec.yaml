name: testapp
description: A new Flutter application.

version: 1.0.0+1

environment:
  sdk: ">=2.4.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  dartx: ^0.2.0
  extended_text: any
  oktoast: ^2.2.1
  provider: 4.0.0

dev_dependencies:
  ff_annotation_route: ^1.1.3+2
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
